@{
    Layout = "_Layout";
    /*This file is executed before any individual file and is therefore used to write any common code.
     This file is heirarchial, meaning we can add this file to  Views folder or it's subfolders like Home.
     If the Layout property is set in the View file itself then that setting overrides the _ViewStart file.
     */
}
